import { BrmaskerDirective } from './brmasker.directive';

describe('BrmaskerDirective', () => {
  it('should create an instance', () => {
    const directive = new BrmaskerDirective();
    expect(directive).toBeTruthy();
  });
});
